---
phase: 04-cli-tool
plan: 05
type: execute
wave: 3
depends_on:
  - 04-04
files_modified: []
autonomous: false
requirements:
  - REQ-401
  - REQ-405
must_haves:
  truths:
    - "npm run build:cli produces dist/cli/index.js that is executable"
    - "dist/cli/index.js --help prints a readable usage summary listing all flags"
    - "All unit tests pass after build"
    - "CLI processes a real image file end-to-end from command line"
  artifacts:
    - path: "dist/cli/index.js"
      provides: "Compiled CLI entry point"
      contains: "img-convert"
  key_links:
    - from: "dist/cli/index.js"
      to: "node_modules/sharp"
      via: "require at runtime"
      pattern: "require.*sharp"
---

<objective>
Wave 3: Build the CLI and verify it works end-to-end with a human in the loop.

Purpose: Confirms the TypeScript compilation succeeds, the binary is executable and shows correct --help output, and the CLI can process a real image file. The human verifies visual/functional correctness that automated tests cannot catch.
Output: Confirmed working dist/cli/index.js.
</objective>

<execution_context>
@/home/dutchbase/.claude/get-shit-done/workflows/execute-plan.md
@/home/dutchbase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-cli-tool/04-04-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build CLI and run full test suite</name>
  <files>dist/cli/index.js</files>
  <action>
Run the following commands in order:

1. Full test suite — must be green before attempting build:
```bash
npx jest
```

2. TypeScript type-check (no emit — quick pass):
```bash
npx tsc --project tsconfig.cli.json --noEmit
```

3. Build the CLI:
```bash
npm run build:cli
```

4. Verify the output exists and is executable:
```bash
ls -la dist/cli/index.js
```

5. Run --help to confirm Commander output:
```bash
node dist/cli/index.js --help
```

6. Spot-check: verify shebang is present in first line:
```bash
head -1 dist/cli/index.js
```

Expected: `#!/usr/bin/env node`

If `npm run build:cli` fails:
- Check TypeScript errors: `npx tsc --project tsconfig.cli.json 2>&1 | head -40`
- Common issue: `@/` path alias not resolving — verify tsconfig.cli.json has `paths: {"@/*": ["./*"]}`
- Common issue: `dom` types leaked — verify `lib: ["ES2020"]` in tsconfig.cli.json
- Fix the issue before proceeding to the checkpoint
  </action>
  <verify>
    <automated>npm run build:cli 2>&1 | tail -5 && ls -la dist/cli/index.js && node dist/cli/index.js --help | head -5</automated>
  </verify>
  <done>npm run build:cli exits 0; dist/cli/index.js exists; node dist/cli/index.js --help prints usage with img-convert name and all flags visible; npx jest exits 0</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verify — CLI end-to-end smoke tests</name>
  <files></files>
  <action>Human verifies CLI behavior end-to-end per the how-to-verify steps below.</action>
  <verify>
    <automated>MISSING — human verify required; automated build check in Task 1</automated>
  </verify>
  <done>Human confirms all 7 smoke test scenarios pass and types "approved".</done>
  <what-built>
    Complete CLI tool: cli/index.ts compiled to dist/cli/index.js. Accepts globs, --format (required), -q/-o/-c short flags, --no-metadata, --quiet. Pipe mode via stdin. Progress output with checkmark/cross per file, summary line, exit codes.
  </what-built>
  <how-to-verify>
    Run these checks in order. Report any step that does not match the expected output.

    1. Help output:
    ```
    node dist/cli/index.js --help
    ```
    Expected: Prints usage with name "img-convert", lists --format (required), -q/--quality, --width, --height, --no-metadata, -o/--output, -c/--concurrency, --quiet.

    2. Missing --format error:
    ```
    node dist/cli/index.js some-file.jpg 2>&1; echo "exit: $?"
    ```
    Expected: Prints "error: required option '-f, --format <fmt>' not specified" and exits non-zero.

    3. Unknown format validation:
    ```
    node dist/cli/index.js -f bmp __tests__/fixtures/test.png 2>&1; echo "exit: $?"
    ```
    Expected: Prints "Error: unknown format 'bmp'" and exits 1.

    4. Unmatched glob warning:
    ```
    node dist/cli/index.js -f webp './no-such-files/*.jpg' 2>&1; echo "exit: $?"
    ```
    Expected: Prints "Warning: no files matched './no-such-files/*.jpg'" and exits non-zero.

    5. Real conversion (requires a test image):
    ```
    node dist/cli/index.js -f webp -o /tmp/cli-test __tests__/fixtures/test.png 2>&1; echo "exit: $?"
    ```
    Expected: Prints "checkmark test.png -> test.webp (N KB -> N KB)" and "Done: 1 converted, 0 failed", exits 0. Check that `/tmp/cli-test/test.webp` exists and is a valid file.

    6. Pipe mode:
    ```
    cat __tests__/fixtures/test.png | node dist/cli/index.js -f webp > /tmp/pipe-test.webp 2>&1; echo "exit: $?"
    ```
    Expected: exits 0, no progress text mixed into the file. Verify: `file /tmp/pipe-test.webp` shows WebP type (or check file size > 0).

    7. Full test suite:
    ```
    npx jest
    ```
    Expected: all suites green, exit 0.
  </how-to-verify>
  <resume-signal>Type "approved" if all 7 checks pass, or describe which step failed and what was printed.</resume-signal>
</task>

</tasks>

<verification>
All 7 manual checks pass. npx jest exits 0. dist/cli/index.js exists and is executable.
</verification>

<success_criteria>
- npm run build:cli exits 0
- dist/cli/index.js starts with #!/usr/bin/env node
- node dist/cli/index.js --help shows all flags including: -f/--format (required), -q/--quality, --width, --height, --no-metadata, -o/--output, -c/--concurrency, --quiet
- Omitting --format shows Commander's built-in required-option error
- Unknown format shows validation error
- Unmatched glob shows warning and exits non-zero
- Real image conversion: checkmark line printed, output file written, exits 0
- Pipe mode: output Buffer written to stdout only, no progress text, exits 0
- npx jest exits 0 (all suites green)
</success_criteria>

<output>
After completion, create `.planning/phases/04-cli-tool/04-05-SUMMARY.md`
</output>
