---
phase: 05-api-polish-dark-mode
plan: "04"
type: execute
wave: 3
depends_on:
  - "05-03"
files_modified: []
autonomous: false
requirements:
  - REQ-501
  - REQ-502

must_haves:
  truths:
    - "Full test suite passes and build is clean before human sign-off"
    - "Dark mode UI verified visually by human with OS dark mode enabled"
    - "Light mode visually unchanged from pre-Phase-5"
    - "API error shapes verified against all 6 new error codes via curl or browser Network tab"
  artifacts: []
  key_links: []
---

<objective>
Gate the phase behind a final automated suite run plus human visual verification of dark mode and API error shapes.

Purpose: Confirm REQ-501 (structured error responses with correct status codes) and REQ-502 (dark mode visual correctness) are complete before closing Phase 5.
Output: Human approval or a list of issues to fix.
</objective>

<execution_context>
@/home/dutchbase/.claude/get-shit-done/workflows/execute-plan.md
@/home/dutchbase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/05-api-polish-dark-mode/05-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full suite gate</name>
  <files></files>
  <action>
Run the full test suite and build. Both must pass clean before presenting the checkpoint.

Commands to run in order:
1. `npm test` — all suites must exit 0
2. `npm run build` — must exit 0 with no TypeScript errors

If either fails, diagnose and fix before proceeding to the checkpoint.
  </action>
  <verify>
    <automated>npm test && npm run build</automated>
  </verify>
  <done>npm test exits 0 (all suites green), npm run build exits 0.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Phase 5 complete:
- REQ-501: `ApiErrorResponse` interface in types/index.ts, `errorResponse()` helper in route.ts, FILE_TOO_LARGE → 413, MISSING_FILE/MISSING_TARGET_FORMAT have field annotations, three new validation guards (INVALID_QUALITY, INVALID_DIMENSION, UNSUPPORTED_TARGET_FORMAT)
- REQ-502: Dark mode via Tailwind dark: variants across app/page.tsx and all 5 components + ImageConverter.tsx
  </what-built>
  <how-to-verify>
**Dark mode (REQ-502):**
1. Set OS to dark mode
2. Run `npm run dev` and open http://localhost:3100
3. Verify ALL of the following show dark surfaces (no white panels):
   - Page background is near-black (neutral-950)
   - Header bar is dark (neutral-900)
   - Drop zone is dark (neutral-900 idle, blue-950 when dragging a file over it)
   - Format selector buttons are dark (neutral-800 background, neutral-200 text)
   - Quality slider area labels are legible (neutral-400 text)
   - Resize inputs have dark background (neutral-800)
   - Metadata checkbox box is dark (neutral-800)
4. Drop 2–3 files: verify batch queue rows are dark (neutral-900), status badges are readable (green-900/green-200 for done, etc.)
5. Complete a conversion: verify ConvertResult success box is dark green (green-950 background, green-200 text)
6. If an image preview shows: verify checkerboard is dark (dark gray tiles, not white/light gray)
7. Switch OS to light mode: verify UI looks identical to pre-Phase-5 (no visual regressions)

**API error shapes (REQ-501):**
With the dev server running, use curl or browser DevTools Network tab to test:
- `curl -X POST http://localhost:3100/api/convert` (no body) → expect 400 with `{"error":"MISSING_FILE","message":"No file provided","field":"file"}`
- Upload a file >50 MB → expect 413 (not 400) with `{"error":"FILE_TOO_LARGE",...}`
- Send quality=999 → expect 400 with `{"error":"INVALID_QUALITY","field":"quality"}`
- Send targetFormat=heic → expect 400 with `{"error":"UNSUPPORTED_TARGET_FORMAT","field":"targetFormat"}`
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe any visual or API issues found.</resume-signal>
</task>

</tasks>

<verification>
Human-verified dark mode and API error shape correctness. Build and tests green.
</verification>

<success_criteria>
- npm test exits 0 and npm run build exits 0
- Human confirms: no white panels in dark mode, all text legible, light mode unchanged
- Human confirms: API error shapes include error/message/field, FILE_TOO_LARGE returns 413
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-polish-dark-mode/05-04-SUMMARY.md`
</output>
