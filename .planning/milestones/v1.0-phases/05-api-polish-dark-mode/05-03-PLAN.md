---
phase: 05-api-polish-dark-mode
plan: "03"
type: execute
wave: 2
depends_on:
  - "05-02"
files_modified:
  - app/page.tsx
  - components/DropZone.tsx
  - components/ConvertOptions.tsx
  - components/ConvertResult.tsx
  - components/ImagePreview.tsx
  - components/BatchQueue.tsx
  - components/ImageConverter.tsx
autonomous: true
requirements:
  - REQ-502

must_haves:
  truths:
    - "With OS dark mode active, no component renders a white or near-white panel against a dark background"
    - "With OS dark mode active, all text is legible (light text on dark backgrounds)"
    - "With OS light mode active, the UI appears visually unchanged from pre-Phase-5"
    - "npm run build exits 0 with all dark: classes applied"
  artifacts:
    - path: "app/page.tsx"
      provides: "Dark shell — page background and header dark variants"
      contains: "dark:bg-neutral-950"
    - path: "components/DropZone.tsx"
      provides: "Dark drag states, dark idle state, dark text"
      contains: "dark:bg-neutral-900"
    - path: "components/ConvertOptions.tsx"
      provides: "Dark format buttons, dark inputs, dark labels, dark metadata box"
      contains: "dark:bg-neutral-800"
    - path: "components/ConvertResult.tsx"
      provides: "Dark success box, dark stat labels and values"
      contains: "dark:bg-green-950"
    - path: "components/ImagePreview.tsx"
      provides: "Dark outer wrapper, dark checkerboard, dark caption bar"
      contains: "dark:bg-neutral-900"
    - path: "components/BatchQueue.tsx"
      provides: "Dark row backgrounds, dark dividers, dark status badges, dark text"
      contains: "dark:bg-neutral-900"
    - path: "components/ImageConverter.tsx"
      provides: "Dark ghost button (Clear queue)"
      contains: "dark:border-neutral-600"
  key_links:
    - from: "app/page.tsx"
      to: "Tailwind dark: variant"
      via: "prefers-color-scheme media query (automatic in Tailwind v4)"
      pattern: "dark:"
---

<objective>
Implement REQ-502: add Tailwind `dark:` variants to all seven affected files so the full UI adapts to the system `prefers-color-scheme: dark` setting. No new packages, no manual toggle — Tailwind v4's automatic dark mode is already active.

Purpose: Users on dark OS themes get a comfortable dark UI with no white panels, correct text contrast, and correct status badge colors throughout.
Output: Seven modified files, each with exhaustive `dark:` class additions per the locked color scale from CONTEXT.md.
</objective>

<execution_context>
@/home/dutchbase/.claude/get-shit-done/workflows/execute-plan.md
@/home/dutchbase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-polish-dark-mode/05-CONTEXT.md
@.planning/phases/05-api-polish-dark-mode/05-RESEARCH.md
@.planning/phases/05-api-polish-dark-mode/05-02-SUMMARY.md

<interfaces>
<!-- Color scale locked by user decisions — executor MUST use these exact values -->

Page background:   dark:bg-neutral-950
Panel / card:      dark:bg-neutral-900
Component inner:   dark:bg-neutral-800  (inputs, metadata box, unselected format buttons)
Borders:           dark:border-neutral-700  (standard), dark:border-neutral-600  (ghost button)
Body text:         dark:text-neutral-100
Muted text:        dark:text-neutral-400
Secondary text:    dark:text-neutral-200  (labels, headings)
Ghost button:      dark:border-neutral-600 dark:text-neutral-300 dark:hover:bg-neutral-800
Primary button:    bg-blue-600 — NO dark: variant needed (passes WCAG AA on neutral-950)

Status badge dark variants (STATUS_BADGE in BatchQueue.tsx):
  pending:    dark:bg-neutral-800 dark:text-neutral-400
  converting: dark:bg-blue-950 dark:text-blue-300
  done:       dark:bg-green-900 dark:text-green-200
  error:      dark:bg-red-900 dark:text-red-200

DropZone states:
  idle:     dark:bg-neutral-900 dark:border-neutral-700
  hover:    dark:hover:bg-neutral-800 dark:hover:border-blue-400
  dragging: dark:bg-blue-950 dark:border-blue-400
  disabled: dark:border-neutral-700 dark:bg-neutral-900  (opacity-50 already on element)

Checkerboard (ImagePreview) dark override:
  dark:bg-[repeating-conic-gradient(#262626_0%_25%,#171717_0%_50%)_0_0/16px_16px]

Number inputs (ConvertOptions) — add bg-white to light, dark variant:
  bg-white dark:bg-neutral-800 dark:border-neutral-600 dark:text-neutral-200

Tailwind v4 note: dark: variants work automatically via prefers-color-scheme.
No tailwind.config.ts changes needed. No class="dark" on <html>.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dark mode — app/page.tsx + DropZone + ConvertOptions</name>
  <files>app/page.tsx, components/DropZone.tsx, components/ConvertOptions.tsx</files>
  <action>
Apply the following dark: class additions to each file. Read each file first before editing.

**app/page.tsx — complete dark mode coverage map:**
- Page container: `min-h-screen bg-neutral-50` → add `dark:bg-neutral-950`
- Header bar `border-b border-neutral-200 bg-white` → add `dark:border-neutral-700 dark:bg-neutral-900`
- H1 heading `text-neutral-900` → add `dark:text-neutral-100`
- Subtitle `text-neutral-500` → add `dark:text-neutral-400`

**components/DropZone.tsx — complete dark mode coverage map:**
- Disabled state classes `border-neutral-200 bg-neutral-50 opacity-50` → add `dark:border-neutral-700 dark:bg-neutral-900`
- Dragging active state `border-blue-500 bg-blue-50` → add `dark:bg-blue-950 dark:border-blue-400`
- Idle/default state `border-neutral-300 hover:border-blue-400 hover:bg-neutral-50` → add `dark:border-neutral-700 dark:bg-neutral-900 dark:hover:bg-neutral-800 dark:hover:border-blue-400`
- Upload icon (SVG) `text-neutral-400` → add `dark:text-neutral-500`
- Drop zone heading (e.g. "Drop image here") `text-neutral-700` → add `dark:text-neutral-200`
- Drop zone subtext (e.g. "or click to browse") `text-neutral-500` → add `dark:text-neutral-400`

**components/ConvertOptions.tsx — complete dark mode coverage map:**
- Section label text `text-neutral-700` (all occurrences) → add `dark:text-neutral-200`
- Format button — unselected state `bg-white text-neutral-700 border-neutral-300 hover:border-blue-400 hover:text-blue-600` → add `dark:bg-neutral-800 dark:text-neutral-200 dark:border-neutral-700 dark:hover:border-blue-400 dark:hover:text-blue-400`
- Format button — same-as-source/disabled state `bg-neutral-100 text-neutral-400 border-neutral-200` → add `dark:bg-neutral-800 dark:text-neutral-600 dark:border-neutral-700`
- AVIF hint text `text-neutral-500` → add `dark:text-neutral-400`
- Quality range labels ("Smaller file" / "Higher quality") `text-neutral-400` → add `dark:text-neutral-500`
- Resize section labels `text-neutral-500` → add `dark:text-neutral-400`
- Number inputs (width/height): add `bg-white dark:bg-neutral-800 dark:border-neutral-600 dark:text-neutral-200` — if the inputs have no explicit `bg-*` class, add `bg-white` to the existing light-mode classes too so the dark override works reliably
- Maintain aspect ratio label `text-neutral-600` → add `dark:text-neutral-300`
- Allow upscaling label `text-neutral-700` → add `dark:text-neutral-200`
- Allow upscaling sub-label `text-neutral-500` → add `dark:text-neutral-400`
- Metadata box container `border-neutral-200 bg-neutral-50` → add `dark:border-neutral-700 dark:bg-neutral-800`
- Metadata label `text-neutral-700` → add `dark:text-neutral-200`
- Metadata sub-label `text-neutral-500` → add `dark:text-neutral-400`

Do NOT add dark: variants to the selected format button (already uses blue-600/blue-50 — keep as-is) or to the primary Convert button (bg-blue-600 passes WCAG AA on both light and dark — no dark: variant needed).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
- app/page.tsx: page container, header, h1, subtitle all have dark: variants
- DropZone.tsx: all three drag states plus text have dark: variants
- ConvertOptions.tsx: all format buttons, labels, inputs, and metadata box have dark: variants
- npm run build exits 0
  </done>
</task>

<task type="auto">
  <name>Task 2: Dark mode — ConvertResult + ImagePreview + BatchQueue + ImageConverter</name>
  <files>components/ConvertResult.tsx, components/ImagePreview.tsx, components/BatchQueue.tsx, components/ImageConverter.tsx</files>
  <action>
Apply the following dark: class additions to each file. Read each file first before editing.

**components/ConvertResult.tsx — complete dark mode coverage map:**
- Success box outer `border-green-200 bg-green-50` → add `dark:border-green-800 dark:bg-green-950`
- Checkmark icon `text-green-600` → add `dark:text-green-400`
- "Conversion complete" text `text-green-800` → add `dark:text-green-200`
- Stat labels ("Original", "Output", "Savings") `text-neutral-500` → add `dark:text-neutral-400`
- Stat values (size numbers) `text-neutral-800` → add `dark:text-neutral-100`
- "Convert another" / reset link `text-neutral-500 hover:text-neutral-800` → add `dark:text-neutral-400 dark:hover:text-neutral-200`

**components/ImagePreview.tsx — complete dark mode coverage map:**
- Outer wrapper `border-neutral-200 bg-neutral-50` (or similar) → add `dark:border-neutral-700 dark:bg-neutral-900`
- Checkerboard background `bg-[repeating-conic-gradient(#e5e7eb_0%_25%,#f9fafb_0%_50%)_0_0/16px_16px]` → add `dark:bg-[repeating-conic-gradient(#262626_0%_25%,#171717_0%_50%)_0_0/16px_16px]`
- Caption bar `border-t border-neutral-200 bg-white` → add `dark:border-neutral-700 dark:bg-neutral-900`
- Filename text `text-neutral-800` → add `dark:text-neutral-100`
- Format/size metadata text `text-neutral-500` → add `dark:text-neutral-400`
- Remove button `text-neutral-400 hover:text-red-500` — leave as-is (red-500 is fine in dark mode)

**components/BatchQueue.tsx — complete dark mode coverage map:**
- Outer border `border-neutral-200` → add `dark:border-neutral-700`
- Row dividers `divide-neutral-100` → add `dark:divide-neutral-700`
- Each row background `bg-white` → add `dark:bg-neutral-900`
- Filename text `text-neutral-800` → add `dark:text-neutral-100`
- Size / aggregate count text `text-neutral-500` → add `dark:text-neutral-400`
- STATUS_BADGE constant — update all four values with dark: variants:
  ```typescript
  const STATUS_BADGE: Record<string, string> = {
    pending:    "bg-neutral-100 text-neutral-500 dark:bg-neutral-800 dark:text-neutral-400",
    converting: "bg-blue-50 text-blue-600 dark:bg-blue-950 dark:text-blue-300",
    done:       "bg-green-50 text-green-700 dark:bg-green-900 dark:text-green-200",
    error:      "bg-red-50 text-red-700 dark:bg-red-900 dark:text-red-200",
  };
  ```
- Remove item button `text-neutral-400 hover:text-neutral-600` → add `dark:text-neutral-500 dark:hover:text-neutral-300`
- Download link `text-blue-600 hover:text-blue-800` → add `dark:hover:text-blue-400`
- Retry button `text-red-600 hover:text-red-800` → add `dark:hover:text-red-400`
- Error message text `text-red-600` → add `dark:text-red-400`

**components/ImageConverter.tsx — complete dark mode coverage map:**
- Clear queue ghost button `border-neutral-300 text-neutral-700 hover:bg-neutral-50` → add `dark:border-neutral-600 dark:text-neutral-300 dark:hover:bg-neutral-800`
- Any other hardcoded light-only color classes visible in the file — scan for `text-neutral-700`, `bg-white`, `border-neutral-200` etc. and apply the standard dark: pairs

Do NOT modify the primary "Convert All" button (bg-blue-600 — no dark: variant needed per locked decisions).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -10</automated>
  </verify>
  <done>
- ConvertResult.tsx: success box, checkmark, stat labels/values, reset link all have dark: variants
- ImagePreview.tsx: wrapper, checkerboard (dark arbitrary gradient), caption bar, text all have dark: variants
- BatchQueue.tsx: row backgrounds, dividers, STATUS_BADGE, text, action links all have dark: variants
- ImageConverter.tsx: ghost button has dark: variants
- npm run build exits 0
  </done>
</task>

</tasks>

<verification>
Run `npm run build` — must exit 0 (confirms no unknown Tailwind class typos cause errors via PostCSS).
Manual verification in the checkpoint plan will follow.
</verification>

<success_criteria>
- All seven files have dark: class additions matching the locked color scale from CONTEXT.md
- No `gray-*` classes introduced (must stay on `neutral-*` scale)
- No `class="dark"` or `data-theme` attribute added to `<html>` or `<body>` elements
- `npm run build` exits 0
- STATUS_BADGE in BatchQueue.tsx uses the `*-900`/`*-200` pattern for done/error and `*-950`/`*-300` for converting
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-polish-dark-mode/05-03-SUMMARY.md`
</output>
