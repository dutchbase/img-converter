---
phase: 05-api-polish-dark-mode
plan: "01"
type: execute
wave: 0
depends_on: []
files_modified:
  - __tests__/route.test.ts
autonomous: true
requirements:
  - REQ-501

must_haves:
  truths:
    - "npm test exits 0 after Wave 0 stubs are added"
    - "Six new it.todo() stubs exist in route.test.ts covering all REQ-501 error shapes"
  artifacts:
    - path: "__tests__/route.test.ts"
      provides: "Wave 0 todo stubs for REQ-501 error shape assertions"
      contains: "it.todo"
  key_links: []
---

<objective>
Add six `it.todo()` stubs to `__tests__/route.test.ts` so every REQ-501 test slot exists before the implementation plan runs.

Purpose: Nyquist compliance — every behavior that will be tested must have a placeholder before code is written. The stubs ensure `npm test` continues to exit 0 during the transition.
Output: Modified `__tests__/route.test.ts` with six new todo stubs in a REQ-501 describe block.
</objective>

<execution_context>
@/home/dutchbase/.claude/get-shit-done/workflows/execute-plan.md
@/home/dutchbase/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-api-polish-dark-mode/05-CONTEXT.md
@.planning/phases/05-api-polish-dark-mode/05-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add REQ-501 it.todo() stubs to route.test.ts</name>
  <files>__tests__/route.test.ts</files>
  <action>
Append a new describe block to `__tests__/route.test.ts` after the existing describe blocks. The block must be titled `"POST /api/convert — REQ-501: structured error responses"` and contain exactly these six it.todo() stubs:

```typescript
describe("POST /api/convert — REQ-501: structured error responses", () => {
  it.todo("returns 413 FILE_TOO_LARGE with { error, message } when file exceeds 50 MB");
  it.todo("returns 400 MISSING_FILE with field: 'file' when no file is attached");
  it.todo("returns 400 MISSING_TARGET_FORMAT with field: 'targetFormat' when targetFormat is absent");
  it.todo("returns 400 INVALID_QUALITY with field: 'quality' when quality is out of range");
  it.todo("returns 400 INVALID_DIMENSION with field: 'resizeWidth' when width is non-positive");
  it.todo("returns 400 UNSUPPORTED_TARGET_FORMAT with field: 'targetFormat' when targetFormat is heic");
});
```

Do NOT modify any existing describe blocks or test cases. Only add the new describe block at the end of the file.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="route" --passWithNoTests</automated>
  </verify>
  <done>npm test exits 0, six new todo stubs visible in route.test.ts, no existing tests broken.</done>
</task>

</tasks>

<verification>
Run `npm test` — all suites must pass (existing tests green, new todos counted as pending not failed).
</verification>

<success_criteria>
- `npm test` exits 0
- `__tests__/route.test.ts` contains exactly six `it.todo()` calls in a REQ-501 describe block
- No previously passing test is broken
</success_criteria>

<output>
After completion, create `.planning/phases/05-api-polish-dark-mode/05-01-SUMMARY.md`
</output>
